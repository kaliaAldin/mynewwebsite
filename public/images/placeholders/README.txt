Placeholder assets live in this folder so the site can run immediately.

Replacement instructions:
1. Export final images to the same filenames to avoid changing code, or update the image paths in `data/projects.ts` and page components.
2. Keep featured cover images close to a 4:3 ratio for project cards.
3. Keep gallery images close to a 16:10 ratio for project pages.
4. Replace `portrait-placeholder.svg` with a portrait image if desired.
5. Replace `public/Ahmed-Isamaldin-CV.txt` with the final CV file before launch.
